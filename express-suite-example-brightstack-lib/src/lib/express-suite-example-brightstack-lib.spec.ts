import { expressSuiteExampleBrightstackLib } from './express-suite-example-brightstack-lib';

describe('expressSuiteExampleBrightstackLib', () => {
  it('should work', () => {
    expect(expressSuiteExampleBrightstackLib()).toEqual(
      'express-suite-example-brightstack-lib',
    );
  });
});
