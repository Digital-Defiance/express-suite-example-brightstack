{
  "name": "express-suite-example-brightchain-react",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "express-suite-example-brightchain-react/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project express-suite-example-brightchain-react --web",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": [
        "^build"
      ],
      "cache": true,
      "outputs": [
        "{workspaceRoot}/dist/express-suite-example-brightchain-react"
      ],
      "options": {
        "cwd": "express-suite-example-brightchain-react",
        "command": "vite build --mode development"
      },
      "configurations": {
        "development": {
          "command": "vite build --mode development"
        },
        "production": {
          "command": "vite build --mode production"
        }
      }
    }
  }
}
