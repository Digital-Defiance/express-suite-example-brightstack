import { expressSuiteExampleBrightstackApiLib } from './express-suite-example-brightstack-api-lib';

describe('expressSuiteExampleBrightstackApiLib', () => {
  it('should work', () => {
    expect(expressSuiteExampleBrightstackApiLib()).toEqual(
      'express-suite-example-brightstack-api-lib',
    );
  });
});
