import { expressSuiteExampleBrightchainLib } from './express-suite-example-brightchain-lib';

describe('expressSuiteExampleBrightchainLib', () => {
  it('should work', () => {
    expect(expressSuiteExampleBrightchainLib()).toEqual(
      'express-suite-example-brightchain-lib',
    );
  });
});
